setwd("U:/BUGS/Case study/Age methylation data")

X_cpg_names <- readRDS("X_cpg_names.rds")

# Check what objects were loaded
ls()

# Assuming the object is X_cpg_names
head(X_cpg_names)
length(X_cpg_names)